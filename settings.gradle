pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // ✅ JitPack for any GitHub-based dependencies
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "SaveIt"
include ':app'
